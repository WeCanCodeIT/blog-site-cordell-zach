class Blog {
    constructor(author, title, body, genre, publishDate, tags) {
      this.author = author;
      this.title = title;
      this.body = body;
      this.genre = genre;
      this.publishDate = publishDate;
      this.tags = tags;
    }
  }
  
  module.exports = Blog;